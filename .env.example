# Database Configuration
# PostgreSQL connection string for the application database
# Format: postgresql://username:password@host:port/database
# Example: postgresql://user:password@localhost:5432/vibemanager
DATABASE_URL=postgresql://user:password@localhost:5432/vibemanager

# Session Secret
# REQUIRED: Must be at least 32 characters long
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-min-32-chars-change-this-in-production

# Server Configuration
# Port number for the application server
# Default: 5000
PORT=5000

# Node Environment
# Options: development, production
# Default: development
NODE_ENV=development

# Application Domain
# The domain where your application is hosted (used for CORS, redirects, etc.)
# Include protocol (http:// or https://)
# Examples: http://localhost:5000, https://yourdomain.com
APP_DOMAIN=http://localhost:5000

# Docker-specific PostgreSQL Configuration (optional - only needed for docker-compose)
# These are used when running with docker-compose to set up the PostgreSQL container
POSTGRES_USER=vibemanager
POSTGRES_PASSWORD=vibemanager
POSTGRES_DB=vibemanager
